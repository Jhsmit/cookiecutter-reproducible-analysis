# {{ cookiecutter.project_name}}


## Installation

To re-run any of the reproducible scripts in this project, either first sync the environment:

```
$ uv sync
```

Or just use `uv run` to run a script and `uv` will automatically use the lockfile to create the environment. 